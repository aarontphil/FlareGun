name: meshlink_app
description: MeshLink — Portable AI-Assisted Offline Communication System
publish_to: 'none'
version: 2.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  web_socket_channel: ^3.0.3
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  uuid: ^4.5.3
  http: ^1.6.0
  provider: ^6.1.5+1
  cupertino_icons: ^1.0.6
  nearby_connections: ^4.3.0
  permission_handler: ^12.0.1
  flutter_gemma: ^0.12.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
